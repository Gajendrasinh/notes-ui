import { FeatureAccessPipe } from './feature-access.pipe';

describe('FeatureAccessPipe', () => {
	it('create an instance', () => {
		const pipe = new FeatureAccessPipe();
		expect(pipe).toBeTruthy();
	});
});
