import { ActiveMeetingDirective } from './active-recording.directive';

describe('ActiveMeetingDirective', () => {
	it('should create an instance', () => {
		const directive = new ActiveMeetingDirective();
		expect(directive).toBeTruthy();
	});
});
